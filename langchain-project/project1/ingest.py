"""STEP 1 · 문서 인제스천 — Load → Split → Embed → Store

PDF(data/*.pdf)를 청크로 쪼개 임베딩해 Chroma(chroma_db/)에 저장한다.
STEP 6의 BM25가 재사용할 수 있도록 build_chunks()를 함수로 분리한다.

→ project1.html STEP 1 코드를 여기에 작성하세요.
"""

if __name__ == "__main__":
    raise SystemExit("아직 미구현: project1.html STEP 1을 참고해 작성하세요.")
