# --- 비밀키 (절대 커밋 금지) ---
.env

# --- 파이썬 / uv ---
.venv/
__pycache__/
*.py[cod]
*.egg-info/

# --- 프로젝트 산출물 (자동 생성) ---
# gitignore는 줄 중간 '#'을 주석이 아닌 패턴의 일부로 본다 → 주석은 반드시 별도 줄에.
cache/
chroma_db/
eval_*.csv

# --- 데이터: PDF 원본은 커밋하지 않음 (폴더 구조만 유지) ---
data/*.pdf
!data/.gitkeep
